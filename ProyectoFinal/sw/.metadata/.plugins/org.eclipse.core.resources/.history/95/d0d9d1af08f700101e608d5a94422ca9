/*
* 	Oscar Fabian Pineda German
* 	Daniel Lafuente Bazo
*/
#include <stdio.h>
#include <stdlib.h>
#include "xparameters.h"
#include "xil_cache.h"
#include "xbasic_types.h"
#include "xil_io.h"
#include "xil_types.h"



// ----------------------- MATRIZ DE PUNTOS ----------------------- //
#include "matriz.h"
#define NUM_ROWS 7
#define NUM_COLS 8
#define MATRIZ_ADDR XPAR_MATRIZ_0_S00_AXI_BASEADDR
#define MATRIZ_REG0 MATRIZ_S00_AXI_SLV_REG0_OFFSET

// Constantes para definir los valores de una fila
// O = Apagado
// T = Encendido
#define OOOOO 0
#define OOOOT 1
#define OOOTO 2
#define OOOTT 3
#define OOTOO 4
#define OOTOT 5
#define OOTTO 6
#define OOTTT 7
#define OTOOO 8
#define OTOOT 9
#define OTOTO 10
#define OTOTT 11
#define OTTOO 12
#define OTTOT 13
#define OTTTO 14
#define OTTTT 15
#define TOOOO 16
#define TOOOT 17
#define TOOTO 18
#define TOOTT 19
#define TOTOO 20
#define TOTOT 21
#define TOTTO 22
#define TOTTT 23
#define TTOOO 24
#define TTOOT 25
#define TTOTO 26
#define TTOTT 27
#define TTTOO 28
#define TTTOT 29
#define TTTTO 30
#define TTTTT 31


u32 MATRIZ_escribir(int fila, int columna, u32 valor){

	if(fila < 0 || NUM_ROWS <= fila){ return valor + 1; }
	if(columna < 0 || NUM_COLS <= columna){ return valor + 1; }


	u32 dato = 0;
	dato = (fila << 11) | (columna << 8) | (valor << 0);

	MATRIZ_mWriteReg(MATRIZ_ADDR, MATRIZ_REG0, dato);
	return dato;
}

void MATRIZ_apagar(){

	for(int f = 0; f < NUM_ROWS; f++){
		for(int c = 0; c < NUM_COLS; c++){
			MATRIZ_escribir(f, c, OOOOO);
		}
	}
}


// -----------------------       VGA        ----------------------- //
#define VGA_DIM 16
void VGA_print(u32 fila, u32 columna, u32 color){

	if(0 < fila || VGA_DIM <= fila)
		return;
	if(0 < columna || VGA_DIM <= columna)
		return;

	u32 valor = (columna << 16) + (fila << 12) + color;
	putfsl(valor, 0);
}

/*
 * Metodo para pintar un numero en la posicion (origenRow, origenCol) en la VGA
 * Cada numero esta representado como se haria en un display de 7 segmentos
 * Cada segmento esta formado por dos pixeles
 */
void VGA_printNumber(int origenRow, int origenCol, u32 color, int number){

	VGA_clearNumber(origenRow, origenCol);

	switch(number){
	case 0:

		// Primera fila
		VGA_print(origenRow - 6, origenCol + 1, color);
		VGA_print(origenRow - 6, origenCol + 2, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol, color);
		VGA_print(origenRow - 5, origenCol + 3, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol, color);
		VGA_print(origenRow - 4, origenCol + 3, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol + 1, color);
		VGA_print(origenRow - 3, origenCol + 2, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol, color);
		VGA_print(origenRow - 2, origenCol + 3, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol, color);
		VGA_print(origenRow - 1, origenCol + 3, color);

		// Septima fila
		VGA_print(origenRow, origenCol + 1, color);
		VGA_print(origenRow, origenCol + 2, color);

		break;

	case 1:

		// Primera fila
		VGA_print(origenRow - 6, origenCol + 3, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol + 3, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol + 3, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol + 3, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol + 3, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol + 3, color);

		// Septima fila
		VGA_print(origenRow, origenCol + 3, color);

		break;

	case 2:

		// Primera fila
		VGA_print(origenRow - 6, origenCol, color);
		VGA_print(origenRow - 6, origenCol + 1, color);
		VGA_print(origenRow - 6, origenCol + 2, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol + 3, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol + 3, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol + 1, color);
		VGA_print(origenRow - 3, origenCol + 2, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol, color);

		// Septima fila
		VGA_print(origenRow, origenCol, color);
		VGA_print(origenRow, origenCol + 1, color);
		VGA_print(origenRow, origenCol + 2, color);

		break;

	case 3:

		// Primera fila
		VGA_print(origenRow - 6, origenCol, color);
		VGA_print(origenRow - 6, origenCol + 1, color);
		VGA_print(origenRow - 6, origenCol + 2, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol + 3, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol + 3, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol, color);
		VGA_print(origenRow - 3, origenCol + 1, color);
		VGA_print(origenRow - 3, origenCol + 2, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol + 3, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol + 3, color);

		// Septima fila
		VGA_print(origenRow, origenCol, color);
		VGA_print(origenRow, origenCol + 1, color);
		VGA_print(origenRow, origenCol + 2, color);

		break;

	case 4:

		// Primera fila
		VGA_print(origenRow - 6, origenCol, color);
		VGA_print(origenRow - 6, origenCol + 3, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol, color);
		VGA_print(origenRow - 5, origenCol + 3, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol, color);
		VGA_print(origenRow - 4, origenCol + 1, color);
		VGA_print(origenRow - 4, origenCol + 2, color);
		VGA_print(origenRow - 4, origenCol + 3, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol + 3, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol + 3, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol + 3, color);

		// Septima fila
		VGA_print(origenRow, origenCol + 3, color);

		break;

	case 5:

		// Primera fila
		VGA_print(origenRow - 6, origenCol, color);
		VGA_print(origenRow - 6, origenCol + 1, color);
		VGA_print(origenRow - 6, origenCol + 2, color);
		VGA_print(origenRow - 6, origenCol + 2, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol, color);
		VGA_print(origenRow - 3, origenCol + 1, color);
		VGA_print(origenRow - 3, origenCol + 2, color);
		VGA_print(origenRow - 3, origenCol + 3, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol + 3, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol + 3, color);

		// Septima fila
		VGA_print(origenRow, origenCol, color);
		VGA_print(origenRow, origenCol + 1, color);
		VGA_print(origenRow, origenCol + 2, color);
		VGA_print(origenRow, origenCol + 3, color);

		break;

	case 6:

		// Primera fila
		VGA_print(origenRow - 6, origenCol, color);
		VGA_print(origenRow - 6, origenCol + 1, color);
		VGA_print(origenRow - 6, origenCol + 2, color);
		VGA_print(origenRow - 6, origenCol + 3, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol, color);
		VGA_print(origenRow - 3, origenCol + 1, color);
		VGA_print(origenRow - 3, origenCol + 2, color);
		VGA_print(origenRow - 3, origenCol + 3, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol, color);
		VGA_print(origenRow - 2, origenCol + 3, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol, color);
		VGA_print(origenRow - 1, origenCol + 3, color);

		// Septima fila
		VGA_print(origenRow, origenCol, color);
		VGA_print(origenRow, origenCol + 1, color);
		VGA_print(origenRow, origenCol + 2, color);
		VGA_print(origenRow, origenCol + 3, color);

		break;

	case 7:

		// Primera fila
		VGA_print(origenRow - 6, origenCol + 1, color);
		VGA_print(origenRow - 6, origenCol + 2, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol, color);

		// Septima fila
		VGA_print(origenRow, origenCol, color);

		break;

	case 8:

		// Primera fila
		VGA_print(origenRow - 6, origenCol + 1, color);
		VGA_print(origenRow - 6, origenCol + 2, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol, color);

		// Septima fila
		VGA_print(origenRow, origenCol, color);

		break;

	case 9:

		// Primera fila
		VGA_print(origenRow - 6, origenCol + 1, color);
		VGA_print(origenRow - 6, origenCol + 2, color);

		// Segunda fila
		VGA_print(origenRow - 5, origenCol, color);

		// Tercera fila
		VGA_print(origenRow - 4, origenCol, color);

		// Cuarta fila
		VGA_print(origenRow - 3, origenCol, color);

		// Quinta fila
		VGA_print(origenRow - 2, origenCol, color);

		// Sexta fila
		VGA_print(origenRow - 1, origenCol, color);

		// Septima fila
		VGA_print(origenRow, origenCol, color);

		break;

	default:
		break;
	}

}

void VGA_clearNumber(int origenRow, int origenCol){

	for(int f = origenRow; f < DIM; f++){
		for(int c = origenCol; c < DIM; c++){
			VGA_print(origenRow - f, c, 0);
		}
	}
}



// -----------------------      KEYPAD      ----------------------- //
#include "keypad.h"
#define KEYPAD_ADDR XPAR_KEYPAD_0_S00_AXI_BASEADDR
#define KEYPAD_REG0 KEYPAD_S00_AXI_SLV_REG0_OFFSET
#define NO_KEY 0xFFFF

/**
 * Funcion bloqueante para leer un caracter del keypad
 */
char KEYPAD_read(){

	KEYPAD_mWriteReg(KEYPAD_ADDR, KEYPAD_REG0, NO_KEY);
	u32 keycode = NO_KEY;

	while( keycode == NO_KEY ){
		keycode = KEYPAD_mReadReg(KEYPAD_ADDR, KEYPAD_REG0);
	}

	if(0 <= keycode && keycode <= 9 )
		return keycode + '0';
	else
		return 'a' + (keycode - 10);
}


// ----------------------        LEDs       ----------------------- //
#include "led.h"
#define LED0_ADDR XPAR_LED_0_S00_AXI_BASEADDR
#define RED_CHANNEL LED_S00_AXI_SLV_REG0_OFFSET
#define GREEN_CHANNEL LED_S00_AXI_SLV_REG1_OFFSET
#define BLUE_CHANNEL LED_S00_AXI_SLV_REG2_OFFSET

#define RED 0
#define GREEN 1
#define BLUE 2

#define LED_UP 255
#define LED_DOWN 0


// Enciende el canal del color 'colLed'
void EnciendeLed(int colLed) {

	xil_printf("\n Encendiendo LED \r\n");
	switch (colLed) {
	case RED:
		LED_mWriteReg(LED0_ADDR, RED_CHANNEL, LED_UP);
		break;

	case GREEN:
		LED_mWriteReg(LED0_ADDR, GREEN_CHANNEL, LED_UP);
		break;

	case BLUE:
		LED_mWriteReg(LED0_ADDR, BLUE_CHANNEL, LED_UP);
		break;

	default:
		xil_printf("\n colLed Corrompido %d \r\n", colLed);
		break;
	}
}

// Apaga el canal del color 'colLed'
void ApagaLed(int colLed) {

	xil_printf("\n Apaga LED \r\n");
	switch (colLed) {
	case RED:
		LEDS_mWriteReg(LED0_ADDR, RED_CHANNEL, LED_DOWN);
		break;

	case GREEN:
		LEDS_mWriteReg(LED0_ADDR, GREEN_CHANNEL, LED_DOWN);
		break;

	case BLUE:
		LEDS_mWriteReg(LED0_ADDR, BLUE_CHANNEL, LED_DOWN);
		break;

	default:
		xil_printf("\n colLed Corrompido %d \r\n", colLed);
		break;
	}
}



void initialize(){

	// Matriz de puntos vacia
	for(int f = 0; f < NUM_ROWS; f++){
		for(int c = 0; c < NUM_COLS; c++){
			MATRIZ_escribir(f, c, 0);
		}
	}

	// LED apagado
	LED_mWriteReg(LED0_ADDR, RED_CHANNEL, LED_DOWN);
	LED_mWriteReg(LED0_ADDR, GREEN_CHANNEL, LED_DOWN);
	LED_mWriteReg(LED0_ADDR, BLUE_CHANNEL, LED_DOWN);

	// VGA en negro
	for(int f = 0; f < NUM_ROWS; f++){
		for(int c = 0; c < NUM_COLS; c++){
			VGA_print(f, c, 0);
		}
	}
}


int main() {

	Xil_ICacheEnable();
	Xil_DCacheEnable();

	initialize();

	Xil_DCacheDisable();
	Xil_ICacheDisable();
	return 0;
}
